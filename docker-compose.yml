version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.front
    ports:
      - 3001:3001
  backend:
    build:
      context: .
      dockerfile: Dockerfile.back
    ports:
      - 3000:3000